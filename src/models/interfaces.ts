export interface DessertInterface {
    id:number
    image: DesertImage
    name: string
    category: string
    price: number
}

export interface DesertImage {
    thumbnail: string
    desktop: string
}

